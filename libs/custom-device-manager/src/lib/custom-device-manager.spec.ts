import { customDeviceManager } from './custom-device-manager';

describe('customDeviceManager', () => {
  it('should work', () => {
    expect(customDeviceManager()).toEqual('custom-device-manager');
  });
});
